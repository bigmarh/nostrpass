import { createEnv } from '@nostrpass/env';

export const env = createEnv({
  NODE_ENV: import.meta.env.MODE || 'development',
  APP_NAME: '{{name}}',
  APP_URL: import.meta.env.VITE_APP_URL || 'http://localhost:{{port}}',
});