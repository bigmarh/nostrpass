{
  "name": "@nostrpass/embassy",
  "version": "1.0.16",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm build && (vite --port 3000 & vite --port 3200 & npx http-server dist -p 3002 --cors -S -C localhost+2.pem -K localhost+2-key.pem)",
    "dev:single": "vite",
    "dev:serve": "npx http-server dist -p 3002 --cors -S -C localhost+2.pem -K localhost+2-key.pem",
    "build": "tsc && vite build",
    "build:lib": "vite build --config vite.config.lib.ts",
    "preview": "vite preview",
    "lint": "eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@nostrpass/env": "workspace:*",
    "@nostrpass/messenger": "workspace:*",
    "@nostrpass/nostrHelpers": "workspace:*",
    "@nostrpass/types": "workspace:*",
    "nostr-tools": "^2.15.2",
    "@noble/secp256k1": "^2.1.0",
    "@noble/curves": "^1.6.0",
    "solid-js": "^1.8.11"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.3.3",
    "vite-plugin-solid": "^2.8.2"
  }
}
