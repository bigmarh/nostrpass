{
  "name": "@nostrpass/messenger",
  "version": "0.0.0",
  "private": true,
  "description": "Simple, secure iframe-to-parent messaging library with middleware support",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.3.3",
    "vitest": "^1.6.0"
  }
}